# Copy to .env.production or set in your host (Render Static Site, etc.) before `npm run build`.
# Your deployed API origin only — no trailing slash, no /api suffix.
VITE_API_URL=https://YOUR-BACKEND.onrender.com
